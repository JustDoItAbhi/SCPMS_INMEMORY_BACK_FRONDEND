spring.application.name=scpminmemory

server.port=${PORT:8080}
app.security.mode=${MODE:production}
#requiresendgrid.from.email=parthsethi29@gmail.com
#sendgrid.from.name=parht
#sendgrid.api.key=${SENDGRID_API_KEY}
#sendgrid.timeout=10000

# EMAIL CONFIGURATION
spring.mail.host=${MAINURL:smtp.gmail.com}
spring.mail.port=${MAIL_PORT:587}
spring.mail.username=${EMAIL:arvinderpalsingh2321@gmail.com}
spring.mail.password=${APP_PASS:jajjfudjbjoujkac}
#  ${APP_PASS:jajj fudj bjou jkac}
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true


spring.profiles.active=dev

#actuator
management.endpoints.web.exposure.include=health,info
management.endpoint.health.show-details=never

# Server Configuration
server.error.include-message=always
server.error.include-stacktrace=on-param
server.error.include-binding-errors=always
server.error.include-exception=true


logging.level.userService=INFO

spring.registerClient.clientId=${CLIENT_ID:abhi}
spring.registerClient.password=${CLIENT_SECRET:abhi123}
spring.registerClient.redirectUri=${REDIRECT_URI:https://scpms-in-memory-frontend-ny3p.onrender.com/callback}
#spring.backend.url=${BACKEND_URL}
spring.backend.url=http://localhost:8080
spring.frontend.url=${FRONTEND_URL:https://scpms-in-memory-frontend-ny3p.onrender.com}
spring.base.url=${spring.frontend.url}
#spring.registerClient.postLogoutRedirectUri=${POST_LOGOUT_URI:http://127.0.0.1:5173}
spring.registerClient.postLogoutRedirectUri=${POST_LOGOUT_URI:https://scpms-in-memory-frontend-ny3p.onrender.com}

spring.security.oauth2.resourceserver.jwt.issuer-uri=${OAUTH_ISSUER_URI}

##to allow different domine requests for exmaple localhost to onrender.com
#server.servlet.session.cookie.same-site=Lax
#server.servlet.session.cookie.secure=false


# LOGGING
logging.level.org.springframework.security=TRACE
logging.level.org.springframework.web=DEBUG

#spring.datasource.hikari.minimum-idle=5
#spring.datasource.hikari.maximum-pool-size=10